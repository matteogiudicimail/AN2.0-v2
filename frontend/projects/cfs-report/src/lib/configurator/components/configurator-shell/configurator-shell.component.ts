/**
 * ConfiguratorShellComponent — top-level admin shell with sidebar nav.
 * Hosts the configurator router-outlet.
 */
import { Component } from '@angular/core';

@Component({
  selector: 'cfg-configurator-shell',
  templateUrl: './configurator-shell.component.html',
})
export class ConfiguratorShellComponent {}
